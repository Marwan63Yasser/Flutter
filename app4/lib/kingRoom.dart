import 'package:flutter/material.dart';

class kroom extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("King Room"),
        backgroundColor: Colors.lightBlue,
      ),
    );
  }
}